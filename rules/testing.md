# Testing Rules
- Unit: validadores/composition engine.
- Integration: repositories/actions/RLS.
- E2E: login, edit, publish, campaign scheduling, rollback, domain resolution.
- Negative tests cross-tenant obligatorios.
- Campaign tests: antes/durante/después, prioridad, timezone, fallback, invalid overrides.
