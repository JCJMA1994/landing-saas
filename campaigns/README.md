# Campaign Presets
Cada preset es configuración segura, no una página independiente. El renderer combina manifest + overrides del tenant + template compatible. Nuevos presets deben pasar revisión visual, accesibilidad, mobile y fallback.
